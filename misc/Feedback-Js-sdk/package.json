{
  "name": "gigatester-js-sdk",
  "version": "1.0.0",
  "private": false,
  "description": "Gigatester feedback modal for browser application",
  "scripts": {
    "clean": "rimraf build/*",
    "build": "node ./src/ObfuscatorUtils.js",
    "copy": "copyfiles -u 1 src/gigatester.css build",
    "deploy:dev": "aws s3 sync build/ s3://dev.gigatester.io/dist/feedback-agent/browser",
    "deploy:beta": "aws s3 sync build/ s3://beta.gigatester.io/dist/feedback-agent/browser"
  },
  "keywords": [
    "gigatester"
  ],
  "files": [
    "lib"
  ],
  "author": "Gigatester",
  "license": "ISC",
  "dependencies": {
    "javascript-obfuscator": "^4.0.0"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "rimraf": "^2.6.3"
  }
}
