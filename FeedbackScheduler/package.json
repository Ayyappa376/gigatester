{
  "name": "feedbackscheduler",
  "version": "1.0.0",
  "description": "Scheduler to upload feedback to external tracking system",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node ./index.js",
    "deploy:dev": "DB_ENV=development npm run build",
    "deploy:beta": "DB_ENV=beta npm run build",
    "deploy:prod": "DB_ENV=production npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.1124.0",
    "axios": "^0.27.2",
    "form-data": "^4.0.0",
    "fs": "0.0.1-security",
    "node-cron": "^3.0.0",
    "node-fetch": "^2.6.7"
  }
}
